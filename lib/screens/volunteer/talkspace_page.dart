import 'package:flutter/material.dart';

class TalkspacePage extends StatelessWidget {
  const TalkspacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Talkspace Page'));
  }
}
